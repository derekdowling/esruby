---
driver:
  name: vagrant
  customize:
    memory: 512
    cpus: 2

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-14.04

suites:
  - name: default
    run_list:
      - recipe[esruby::default]
    attributes:
